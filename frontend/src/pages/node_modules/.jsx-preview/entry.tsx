
    import React from "react";
    import { createRoot } from "react-dom/client";
    import { useAuth } from "c:\Users\gur5811\std\IFQ636 Software Lifecycle Management\Assignment 1\taskmanagerv1\frontend\src\context\AuthContext";
import { useNavigate } from 'react-router-dom';
import axiosInstance from "c:\Users\gur5811\std\IFQ636 Software Lifecycle Management\Assignment 1\taskmanagerv1\frontend\src\axiosConfig";
    

    const Preview = () => {
      return (
            <div className="max-w-md mx-auto mt-20">
      <form onSubmit={handleSubmit} className="bg-white p-6 shadow-md rounded">
        <h1 className="text-2xl font-bold mb-4 text-center">Login</h1>
        <input
          type="email"
          placeholder="Email"
          value={formData.email}
          onChange={(e) => setFormData({ ...formData, email: e.target.value })}
          className="w-full mb-4 p-2 border rounded"
        />
        <input
          type="password"
          placeholder="Password"
          value={formData.password}
          onChange={(e) => setFormData({ ...formData, password: e.target.value })}
          className="w-full mb-4 p-2 border rounded"
        />
        <button type="submit" className="w-full bg-blue-600 text-white p-2 rounded">
          Login
        </button>
      </form>
    </div>
      );
    };

    const root = createRoot(document.getElementById("root"));
    root.render(<Preview />);
  